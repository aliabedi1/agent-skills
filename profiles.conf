# Profiles map to collection directories. Keep this file dependency-free so both
# installers can read it without a JSON/YAML parser.
minimal=custom
backend=matt,custom
frontend=cursor,custom
matt=matt
cursor=cursor
all=all
